{
  "name": "remotetail",
  "version": "1.0.0",
  "description": "SSH-based remote log tail viewer for Windows",
  "scripts": {
    "dev":   "cargo tauri dev",
    "build": "cargo tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
