{
  "name": "remotetail",
  "version": "2.2.0",
  "description": "SSH-based remote log tail viewer for Windows",
  "scripts": {
    "dev":   "npx tauri dev",
    "build": "npx tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
